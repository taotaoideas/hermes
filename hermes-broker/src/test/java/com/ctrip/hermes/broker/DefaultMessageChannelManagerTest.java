package com.ctrip.hermes.broker;

import org.junit.Test;
import org.unidal.lookup.ComponentTestCase;

public class DefaultMessageChannelManagerTest extends ComponentTestCase {

	@Test
	public void test() {
		//		DefaultMessageChannelManager m = (DefaultMessageChannelManager) lookup(MessageChannelManager.class);
	}

}
